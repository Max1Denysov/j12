public class D {
    public static void main (String []args){
     B myb = new B(12,13,15);
     myb.show();
     myb.show("This is  K:");
    }
}
//Если метод подкласса и суперкласса имеют одинаковые имена и типы входных данных то метод суперласса
// Переопределяется и скрывается.Вызывается метод подкласса . Чтобы вызвать Метод суперкласса нужно испозьзовать ключевое слово метод.super
// Если у методов разные типы данных то метод перегружается.